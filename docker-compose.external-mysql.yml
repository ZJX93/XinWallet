# ============================================
# 鑫钱包 · 复用已有 MySQL 的部署（模式 B — MySQL）
# 仅启动应用容器，连接你 NAS / 服务器上「已经存在的 MySQL 8.0+」
# （复用既有数据，不另起库实例）
#
# 用法：
#   docker compose -f docker-compose.external-mysql.yml up -d          # 默认拉取 GHCR 预构建镜像
#   docker compose -f docker-compose.external-mysql.yml up -d --build  # 从本地源码构建
#
# 数据库连接信息全部来自同目录 .env（DB_HOST / DB_PORT / DB_USER /
# DB_PASSWORD / DB_NAME）。请确保该账号具备建表/改表权限。
# ============================================

services:
  app:
    image: ghcr.io/zjx93/xin-wallet/xinwallet:latest
    build: .
    container_name: xinwallet-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 18888
      DB_DIALECT: mysql
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME:-xinwallet}
      JWT_SECRET: ${JWT_SECRET:-please-change-this-secret}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      DEMO_PASSWORD: ${DEMO_PASSWORD}
      CORS_ORIGIN: ${CORS_ORIGIN}
    ports:
      - "${APP_PORT:-18888}:18888"
    extra_hosts:
      - "host.docker.internal:host-gateway"
